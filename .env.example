# HubSpot Private App Token (EU instance)
HUBSPOT_TOKEN=pat-eu1-xxxx-xxxx-xxxx-xxxxxxxxxxxx
HUBSPOT_PORTAL_ID=26464920

# Persistent storage directory for supplier templates.
# Set this to a Railway volume mount path (e.g. /data) so templates survive
# redeployments. Defaults to the app directory (ephemeral on Railway).
# TEMPLATES_DIR=/data
